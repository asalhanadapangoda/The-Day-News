{
  "name": "the-day-news-v2",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix client run build"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "@vercel/speed-insights": "^2.0.0"
  }
}
